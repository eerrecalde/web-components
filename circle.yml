general:
  branches:
    ignore:
      - gh-pages

test:
  override:
    - exit 0

dependencies:
  pre:
    - cd wc-v1-es5
    - rm -Rf bower_components
    - npm install -g bower
    - bower install

deployment:
  staging:
    branch: master
    commands:
      - ./scripts/deploy-ghpages.sh src
