general:
  branches:
    ignore:
      - gh-pages

test:
  override:
    - exit 0

dependencies:
  pre:
    - npm install -g bower
    - bower install

deployment:
  staging:
    branch: master
    commands:
      - ./scripts/deploy-ghpages.sh src
